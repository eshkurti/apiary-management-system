<?php

declare(strict_types=1);

// Intentionally empty: the order table is reset to a clean slate before each
// checkout test so neither demo-seed rows nor an order created by an earlier
// test can bleed into the next one.
return [];
